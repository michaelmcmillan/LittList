{{> searchbar }}

<div class="list">
    <div class="settings">
        <form method="post" action="/liste">
            <select name="style">
                {{#each supportedStyles}}
                <option {{#if selected}} selected {{/if}} value="{{file}}">{{name}}</option>
                {{/each}}
            </select>
            <button class="change-style" type="submit">Bytt referansestil</button>
        </form>
    </div>
    <div class="a4">
        <form method="post" action="/liste">
            <h2>Litteraturliste</h2>
            {{{list}}}
        </form>
    </div>
</div>
